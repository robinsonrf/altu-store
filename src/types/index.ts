/**
 * Tipos compartidos de la app. El dominio puro sigue en `@/domain`.
 */
export type {
  Money,
  Product,
  ProductCategory,
  ProductColor,
  ProductFilters,
  ProductId,
  ProductImage,
} from "@/domain/product";
